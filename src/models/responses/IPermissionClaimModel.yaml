components:
  schemas:
    IPermissionClaimModel:
      type: object
      properties:
        baseClaimName:
          type: string
          pattern: '[a-zA-Z0-9_-]+'
          nullable: true
        readClaim:
          type: string
          pattern: '[a-zA-Z0-9_-]+'
          nullable: true
        scheduleClaim:
          type: string
          pattern: '[a-zA-Z0-9_-]+'
          nullable: true
        manageClaim:
          type: string
          pattern: '[a-zA-Z0-9_-]+'
          nullable: true
