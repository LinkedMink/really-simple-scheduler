components:
  schemas:
    IProgressModel:
      type: object
      properties:
        completedRatio:
          type: number
          minimum: 0
          maximum: 1
        estimatedCompletedBy:
          type: string
          format: date-time
          nullable: true
        resultSample:
          type: object
          nullable: true
