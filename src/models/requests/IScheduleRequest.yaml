components:
  schemas:
    IScheduleRequest:
      type: object
      properties:
        type:
          type: string
          pattern: '[a-zA-Z0-9_-]+'
        parameters:
          type: object
          nullable: true
